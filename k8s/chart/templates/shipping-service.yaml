{{- $namespace := .Values.namespace | default "development" }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: shipping-service
  namespace: {{ $namespace }}
  labels:
    app: shipping-service
    component: business
spec:
  replicas: {{ .Values.shippingService.replicaCount | default 1 }}
  selector:
    matchLabels:
      app: shipping-service
  template:
    metadata:
      labels:
        app: shipping-service
        component: business
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8600"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      containers:
      - name: shipping-service
        image: {{ .Values.shippingService.image | default "ecommerceacr1202.azurecr.io/shipping-service:0.1.0" }}
        ports:
        - containerPort: 8600
          name: http
          protocol: TCP
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: {{ .Values.shippingService.springProfile | default "dev" | quote }}
        - name: SPRING_ZIPKIN_BASE_URL
          value: {{ .Values.shippingService.zipkinUrl | default "http://zipkin:9411" | quote }}
        - name: EUREKA_CLIENT_REGION
          value: "default"
        - name: EUREKA_CLIENT_AVAILABILITYZONES_DEFAULT
          value: "myzone"
        - name: EUREKA_INSTANCE_PREFER_IP_ADDRESS
          value: "true"
        - name: EUREKA_CLIENT_SERVICEURL_MYZONE
          value: {{ .Values.shippingService.eurekaMyZone | default "http://service-discovery:8761/eureka" | quote }}
        - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
          value: {{ .Values.shippingService.eurekaDefaultZone | default "http://service-discovery:8761/eureka/" | quote }}
        - name: SPRING_CONFIG_IMPORT
          value: {{ .Values.shippingService.springConfigImport | default "optional:configserver:http://cloud-config:9296/" | quote }}
        - name: JAVA_OPTS
          value: {{ .Values.shippingService.javaOpts | default "-Xms256m -Xmx512m -XX:+UseG1GC" | quote }}
        - name: MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE
          value: "*"
        - name: SPRING_SECURITY_ENABLED
          value: "false"
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /shipping-service/actuator/health/liveness
            port: 8600
          initialDelaySeconds: 90
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /shipping-service/actuator/health/readiness
            port: 8600
          initialDelaySeconds: 30
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: shipping-service
  namespace: {{ $namespace }}
  labels:
    app: shipping-service
    component: business
spec:
  selector:
    app: shipping-service
  ports:
    - protocol: TCP
      port: 8600
      targetPort: 8600
  type: ClusterIP
