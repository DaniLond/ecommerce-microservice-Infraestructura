{{- $namespace := .Values.namespace | default "development" }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: favourite-service
  namespace: {{ $namespace }}
  labels:
    app: favourite-service
    component: business
spec:
  replicas: {{ .Values.favouriteService.replicaCount | default 1 }}
  selector:
    matchLabels:
      app: favourite-service
  template:
    metadata:
      labels:
        app: favourite-service
        component: business
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8800"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      containers:
      - name: favourite-service
        image: {{ .Values.favouriteService.image | default "ecommerceacr1202.azurecr.io/favourite-service:0.1.0" }}
        ports:
        - containerPort: 8800
          name: http
          protocol: TCP
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: {{ .Values.favouriteService.springProfile | default "dev" | quote }}
        - name: SPRING_ZIPKIN_BASE_URL
          value: {{ .Values.favouriteService.zipkinUrl | default "http://zipkin:9411" | quote }}
        - name: EUREKA_CLIENT_REGION
          value: "default"
        - name: EUREKA_CLIENT_AVAILABILITYZONES_DEFAULT
          value: "myzone"
        - name: EUREKA_INSTANCE_PREFER_IP_ADDRESS
          value: "true"
        - name: EUREKA_CLIENT_SERVICEURL_MYZONE
          value: {{ .Values.favouriteService.eurekaMyZone | default "http://service-discovery:8761/eureka" | quote }}
        - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
          value: {{ .Values.favouriteService.eurekaDefaultZone | default "http://service-discovery:8761/eureka/" | quote }}
        - name: SPRING_CONFIG_IMPORT
          value: {{ .Values.favouriteService.springConfigImport | default "optional:configserver:http://cloud-config:9296/" | quote }}
        - name: JAVA_OPTS
          value: {{ .Values.favouriteService.javaOpts | default "-Xms256m -Xmx512m -XX:+UseG1GC" | quote }}
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /favourite-service/actuator/health/liveness
            port: 8800
          initialDelaySeconds: 90
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /favourite-service/actuator/health/readiness
            port: 8800
          initialDelaySeconds: 30
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: favourite-service
  namespace: {{ $namespace }}
  labels:
    app: favourite-service
    component: business
spec:
  selector:
    app: favourite-service
  ports:
    - protocol: TCP
      port: 8800
      targetPort: 8800
  type: ClusterIP
