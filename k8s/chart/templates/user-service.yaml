# User Service (Helm template)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: {{ .Values.namespace }}
  labels:
    app: user-service
    component: business
spec:
  replicas: {{ .Values.userService.replicas | default 1 }}
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
        component: business
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8700"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      containers:
      - name: user-service
        image: {{ .Values.userService.image }}
        ports:
        - containerPort: 8700
          name: http
          protocol: TCP
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: {{ .Values.userService.springProfile | quote }}
        - name: SPRING_ZIPKIN_BASE_URL
          value: {{ .Values.userService.zipkinUrl | quote }}
        - name: EUREKA_CLIENT_REGION
          value: "default"
        - name: EUREKA_CLIENT_AVAILABILITYZONES_DEFAULT
          value: "myzone"
        - name: EUREKA_INSTANCE_PREFER_IP_ADDRESS
          value: "true"
        - name: EUREKA_CLIENT_SERVICEURL_MYZONE
          value: {{ .Values.userService.eurekaMyZone | quote }}
        - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
          value: {{ .Values.userService.eurekaDefaultZone | quote }}
        - name: SPRING_CONFIG_IMPORT
          value: {{ .Values.userService.configImport | quote }}
        - name: JAVA_OPTS
          value: {{ .Values.userService.javaOpts | quote }}
        - name: MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE
          value: "*"
        - name: SPRING_SECURITY_ENABLED
          value: "false"
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /user-service/actuator/health/liveness
            port: 8700
            httpHeaders:
            - name: Authorization
              value: Basic YWRtaW46YWRtaW4=
          initialDelaySeconds: 120
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /user-service/actuator/health/readiness
            port: 8700
            httpHeaders:
            - name: Authorization
              value: Basic YWRtaW46YWRtaW4=
          initialDelaySeconds: 90
          periodSeconds: 5
        startupProbe:
          httpGet:
            path: /user-service/actuator/health
            port: 8700
            httpHeaders:
            - name: Authorization
              value: Basic YWRtaW46YWRtaW4=
          initialDelaySeconds: 30
          periodSeconds: 10
          failureThreshold: 30
---
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: {{ .Values.namespace }}
  labels:
    app: user-service
spec:
  type: ClusterIP
  ports:
  - port: 8700
    targetPort: 8700
    protocol: TCP
    name: http
  selector:
    app: user-service
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: user-service-hpa
  namespace: {{ .Values.namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: user-service
  minReplicas: {{ .Values.userService.hpa.minReplicas | default 1 }}
  maxReplicas: {{ .Values.userService.hpa.maxReplicas | default 8 }}
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
